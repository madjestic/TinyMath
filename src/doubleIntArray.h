// doubleIntArray.h

// Given an array of integers and its length,
// returns a pointer to a newly allocated array with each value doubled.
// The caller is responsible for freeing the returned array.
int* doubleIntArray(const int array[], int length);
